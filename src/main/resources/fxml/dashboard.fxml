<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx"
      xmlns:fx="http://javafx.com/fxml"
      fx:controller="controller.DashboardController"
      spacing="10" alignment="TOP_CENTER" style="-fx-padding: 20;">

    <HBox spacing="15">
        <Button text="Accueil" onAction="#showHome"/>
        <Button text="Films" onAction="#showFilms"/>
        <Button text="Recherche" onAction="#showSearch"/>
        <Button text="Profil" onAction="#showProfile"/>
        <Button text="Déconnexion" onAction="#logout"/>
    </HBox>

    <Label fx:id="welcomeLabel" style="-fx-font-size: 18px; -fx-font-weight: bold;"/>
    <Label fx:id="userEmailLabel"/>

    <VBox fx:id="mainContent" spacing="10"/>
</VBox>
